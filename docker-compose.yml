# DEVELOPMENT DOCKER COMPOSE
services:
  web:
    container_name: web-app
    build:
        context: ./web
        dockerfile: Dockerfile
    ports:
        - "3000:3000"
    volumes:
        - ./web:/usr/src/app
